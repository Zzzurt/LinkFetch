# 必填：客户端需在设置中填入相同的 Token。
# 未配置时服务会拒绝所有解析请求（503），不会再静默放开。
API_TOKEN=

# 可选：仅本机调试且确实不想配 Token 时设为 1，显式承担无鉴权风险。
# 无鉴权意味着任何能访问该端口的人都能用它代发平台请求（可自带 Cookie）。
ALLOW_UNAUTHENTICATED=

# 可选：平台 Cookie（应对部分受限内容，例如登录后才能查看的笔记）
XHS_COOKIE=
DOUYIN_COOKIE=
WEIBO_COOKIE=

# 可选：允许跨域的来源白名单，逗号分隔（如 https://example.com）。
# 默认不开放跨域——移动端不依赖同源策略，开放只会让任意网页把本服务当免费解析代理。
CORS_ALLOW_ORIGINS=
