# Python
__pycache__/
*.py[cod]
.venv/
backend/.venv/
.pytest_cache/

# 环境变量（含 Cookie / Token，不入库）
.env
backend/.env

# 内部架构文档（仅本地，不公开）
CODE_WIKI.md

# Android / Gradle
.gradle/
build/
android/build/
android/app/build/
android/local.properties
android/.kotlin/
*.iml
.idea/
captures/
*.apk
*.aab

# 系统
.DS_Store
Thumbs.db
# 签名密钥与配置（含口令，绝不入库）
.keystore/
keystore.properties
*.keystore
*.jks
# 本机签名密钥与 agent 工作记录（不入库）
.workbuddy/

# 内部交付记录（仅本地，不公开）
UI_OPTIMIZATION_DELIVERY.md
# 内部代码评审记录（含未修复问题细节，修复后再决定是否公开）
CODE_REVIEW_*.md
UI_REVIEW_*.md
# 内部 UI 设计方案与提案（含未采纳的备选方案，实施后再决定是否公开）
UI_DESIGN_*.md
